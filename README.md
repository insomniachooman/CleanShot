
# CleanShotX for Windows (TypeScript, Electron)

A Windows desktop application that emulates CleanShot X style screen capture features with pixel-precise region selection, window and display capture, scrolling capture, and a beautiful overlay UI.

Quick start
- npm install
- npm run dev

Build
- npm run dist

See docs/architecture.md and docs/ui-spec.md for full details.
    